# Зависимости приложения (десктоп + мобильная сборка)
flet==0.25.2
python-dotenv>=1.0.0
requests>=2.28.0

# [MOBILE] psutil — нативная библиотека, на Android/iOS через flet build не собирается.
# Мониторинг производительности работает и без неё (см. utils/monitor.py).
psutil>=5.9.0; platform_system != "Android" and platform_system != "iOS"
